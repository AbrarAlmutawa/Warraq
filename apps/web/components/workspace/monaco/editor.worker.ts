/*
 * Local Monaco editor worker.
 * The bundler compiles this file (and Monaco's worker code it imports)
 * into a same-origin worker file — nothing is fetched from a CDN.
 */
import "monaco-editor/esm/vs/editor/editor.worker";